server {
    listen 8080;
    server_name ministry-web;
    location = /healthz { add_header Content-Type text/plain; return 200 "ok\n"; }
    location / { add_header Content-Type text/plain; return 200 "ministry-web: giving portal up\n"; }
}
